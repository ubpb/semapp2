<div class="panel info ui-corner-all">
  <h3>
    <%= @sem_app.title %>
    <% if current_user and current_user.is_admin? %>
    <span>(<%= link_to 'administrieren', edit_admin_sem_app_url(@sem_app) %>)</span>
    <% end %>
  </h3>
  <div>
    <strong>Semester:</strong> <%= h(@sem_app.semester.title) %>
  </div>
  <div><strong>Dozent/in:</strong> xxx</div>
</div>

<%= render 'sem_app', :sem_app => @sem_app %>
