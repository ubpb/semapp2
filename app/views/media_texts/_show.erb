<%= render 'media/publish_notice', :media => media %>

<% if media.present? and media.text.present? %>
  <%= textilize(media.text).html_safe %>
<% end %>

<%= render 'media/attachments', :media => media %>
