<%= entry.to_s %>

<% if entry.scanjob.present? %>
  <i>Scan wird in kürze bereitgestellt</i>
<% else %>
  <%= link_to 'Einen Scan beaufragen', new_monograph_entry_scanjob_path(entry) %>
<% end %>

<%= render 'entries/attachments', :entry => entry %>