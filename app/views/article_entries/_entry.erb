<%= entry.to_s %>

<%= render 'entries/signature', :entry => entry %>

<%= render 'entries/amazon', :entry => entry %>

<%= render 'entries/comment', :entry => entry %>

<%= render 'entries/scan', :entry => entry %>

<%= render 'entries/attachments', :entry => entry %>